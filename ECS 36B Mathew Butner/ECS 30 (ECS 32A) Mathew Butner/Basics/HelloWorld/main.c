#include <stdio.h> // include the standard input and output library
                  // so that we can print stuff to the screen

/*
 * All programs start at main
 * And like highlander there can be only one
 */
int main() {
  // a string literal is anything enclosed in " "
  printf("Hello, World!\n"); // printf is used to display text to the screen
  return 0; // 0 means success when returned from main
}